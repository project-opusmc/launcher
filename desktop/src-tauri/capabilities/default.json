{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the Opus Launcher window.",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-close"]
}
